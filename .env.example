# 服务器配置
NODE_ENV=development
PORT=3000

# 日志配置
# DEBUG_LEVEL: 控制日志输出级别
# 可选值: debug (显示所有), info (常规信息), warn (警告), error (仅错误), none (不显示)
# 默认: debug (开发环境) / info (生产环境)
DEBUG_LEVEL=debug

# ocloudview API配置
OCLOUDVIEW_API_URL=http://192.168.40.161:8088
OCLOUDVIEW_API_KEY=your_api_key_here

# JWT配置
JWT_SECRET=your_jwt_secret_here
JWT_EXPIRES_IN=24h

# WebSocket配置
WEBSOCKET_PORT=6080
WEBSOCKET_PATH=/vnc

# VNC配置
VNC_PASSWORD_ENCRYPTION=true

# 连接超时配置（毫秒）
# 对于高延迟网络场景，建议设置更长的超时时间
# SPICE 协议需要建立多达 17+ 个通道，在高延迟场景下需要足够的时间
# 默认: 30000 (30秒)
# 高延迟场景建议: 60000-120000 (60-120秒)
CONNECTION_TIMEOUT=30000

# 连接重试配置
# 在网络不稳定或高延迟场景下，自动重试可以提高连接成功率
# MAX_RETRIES: 最大重试次数（默认: 3，即总共尝试 4 次）
# RETRY_DELAY: 初始重试延迟（毫秒，默认: 1000）
# RETRY_BACKOFF_MULTIPLIER: 重试延迟倍数（默认: 2，即每次重试延迟翻倍）
# 重试延迟计算: delay = RETRY_DELAY * (RETRY_BACKOFF_MULTIPLIER ^ retry_count)
# 例如: 1s -> 2s -> 4s（3次重试）
MAX_RETRIES=3
RETRY_DELAY=1000
RETRY_BACKOFF_MULTIPLIER=2

# SPICE 个性化配置
# 这些参数控制 SPICE 协议的连接质量和性能
# SPICE_BANDWIDTH_LIMIT: 带宽限制（默认: 12）
# SPICE_FRAME_RATE: 帧率（默认: 25）
# SPICE_DECODE_PIX_FORMAT: 解码像素格式（默认: 1）
# SPICE_DECODE_TYPE: 解码类型（默认: 1）
# SPICE_ENCODE_FORMAT: 编码格式（默认: 0）
# SPICE_GAME_MODE: 游戏模式（默认: 1，启用）
# SPICE_MOUSE_MODE: 鼠标模式（默认: 0）
SPICE_BANDWIDTH_LIMIT=12
SPICE_FRAME_RATE=25
SPICE_DECODE_PIX_FORMAT=1
SPICE_DECODE_TYPE=1
SPICE_ENCODE_FORMAT=0
SPICE_GAME_MODE=1
SPICE_MOUSE_MODE=0

# 数据库配置（如需要）
DB_HOST=localhost
DB_PORT=3306
DB_NAME=novnc_ocloudview
DB_USER=root
DB_PASSWORD=

# Redis配置（用于会话管理）
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
